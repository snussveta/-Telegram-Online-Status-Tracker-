
# 🟢 Telegram Online Status Tracker 📊

Простая Python-программа, отслеживающая **статус Telegram-пользователя** (онлайн/оффлайн) в реальном времени и записывающая его в **Google Таблицу**.  
Также реализован встроенный Flask-сервер для поддержания активности на хостингах вроде Heroku или Replit.

---

## 📁 Описание проекта

### 🔍 Возможности

- ⏱️ Проверка статуса указанного Telegram-пользователя каждые 3 секунды
- 📉 Запись истории статуса в Google Sheets с временной меткой
- 🌍 Поддержка часового пояса
- 🌐 Flask-сервер для keep-alive

---

## 🚀 Быстрый старт

### 1. Клонируйте репозиторий:

```bash
git clone https://github.com/your-username/telegram-status-tracker.git
cd telegram-status-tracker
```

### 2. Установите зависимости

Создайте виртуальное окружение и установите зависимости:

```bash
pip install -r requirements.txt
```

### 3. Настройте Telegram API

Зарегистрируйте свое приложение на [my.telegram.org](https://my.telegram.org):

- Получите `api_id` и `api_hash`
- Укажите свой номер телефона

🔐 **Пример (замените своими данными в `main.py`)**:

```python
api_id = 12345678
api_hash = 'your_api_hash_here'
phone = '+1234567890'
username = 'example_user_or_id'  # username или ID пользователя
```

### 4. Настройте доступ к Google Sheets

- Создайте сервисный аккаунт в Google Cloud
- Дайте ему доступ на редактирование вашей таблицы
- Скачайте файл `credentials.json` и поместите его в корень проекта

🛡️ **Важно**: в репозитории не публикуйте реальные ключи. Используйте `.gitignore` для `credentials.json`.

**Пример конфигурации (`credentials.json`)**:
```json
{
  "type": "service_account",
  "project_id": "example-project-id",
  ...
}
```

### 5. Создайте Google Таблицу

- Назовите таблицу, например: `tgstatuslogin`
- Название задается в `main.py`:
```python
sheet_name = 'tgstatuslogin'
```

---

## 🖥️ Запуск

```bash
python main.py
```

При первом запуске потребуется ввести код подтверждения, полученный в Telegram.

---

## 🌐 Веб-сервер Flask

Для keep-alive на Replit, Heroku и аналогичных платформах:

- Flask-сервер запускается на `0.0.0.0:8080`
- Проверьте в браузере: [http://localhost:8080](http://localhost:8080)

Ответ:  
```
Бот работает!
```

---

## 🛠️ Структура проекта

```
📁 telegram-status-tracker/
├── main.py               # Основной скрипт
├── credentials.json      # 🔐 Доступ к Google Sheets (НЕ публиковать!)
├── requirements.txt      # Зависимости проекта
├── Procfile              # Для запуска на Heroku
└── README.md             # Документация
```

---

## 📦 Зависимости

Указаны в `requirements.txt`. Ключевые библиотеки:

- `Telethon` — Telegram API
- `gspread` + `oauth2client` — для работы с Google Таблицами
- `Flask` — HTTP сервер
- `pytz` — поддержка часовых поясов
- `logging` — логирование в консоль

---

## 🧠 Как это работает?

Каждые 3 секунды:

1. Получается статус указанного пользователя
2. Статус конвертируется в `онлайн`, `оффлайн (был ...)` или `недоступно`
3. Если статус изменился — он записывается в Google Таблицу
4. Все изменения логируются в консоль

---

## ⚠️ Безопасность

- Все **конфиденциальные данные заменены** в этом проекте на примеры.
- Настоятельно рекомендуется использовать `.env` и `dotenv` для хранения API-ключей.
- Не публикуйте файл `credentials.json` и `session_data.session`.

Добавьте в `.gitignore`:
```
credentials.json
session_data.session*
```

---

## 🧪 Пример результата

Пример строки в Google Таблице:

| Время                  | Статус                         |
|------------------------|--------------------------------|
| 2025-07-20 17:03:12     | онлайн                         |
| 2025-07-20 17:05:44     | оффлайн (был 2025-07-20 17:04:10) |

---

## 🤝 Контрибуция

- Форкайте
- Создавайте ветки
- Присылайте PR

---

## 📄 Лицензия

MIT License
